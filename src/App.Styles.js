import styled from "styled-components";

export const Main = styled.div`
  z-index: 0;
  overflow: hidden;
`;

export const MainView = styled.div`
  position: relative;
  min-height: 1000px;
  z-index: 0;
`;
