import styled from "styled-components";

import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export const ActionIconMain = styled(FontAwesomeIcon)`
  && {
    color: #fff;
    fill: #fff;
    width: 1rem;
    height: 1rem;
    overflow: hidden !important;
  }
`;
